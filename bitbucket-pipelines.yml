image: node:8.4.0

pipelines:
  default:
    - step:
        caches:
          - node
        script:
          - npm install
          - npm run lint
          - npm run coverage
          - npm run storybook
          - npm run build
          - npm run website:publish
