image: node:8.4.0

pipelines:
  default:
    - step:
        caches:
          - node
        script:
          - npm install
          - npm run lint
          - npm publish-coverage-report
          - npm publish-website
